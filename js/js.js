/*$('#view-flasker').on('click', function() {
    const flasker = $('#flasker').position().top;

    $('html, body').animate(
        {
            scrollTop: flasker
        },
        900
    );
});


$('#view-daaser').on('click', function() {
    const flasker = $('#daaser').position().top;

    $('html, body').animate(
        {
            scrollTop: daaser
        },
        900
    );
});


$('#view-tilbud').on('click', function() {
    const tilbud = $('#tilbud').position().top;

    $('html, body').animate(
        {
            scrollTop: tilbud
        },
        900
    );
});*/